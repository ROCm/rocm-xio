# DKMS configuration for bnxt_re (rocm-xio)
#
# SPDX-License-Identifier: GPL-2.0

PACKAGE_NAME="rocm-xio-bnxt-re"
PACKAGE_VERSION="1.0.0"

BUILT_MODULE_NAME[0]="bnxt_re"
DEST_MODULE_LOCATION[0]="/updates/dkms"

AUTOINSTALL="yes"

MAKE="make -C ${kernel_source_dir} \
  M=${dkms_tree}/${PACKAGE_NAME}/${PACKAGE_VERSION}/build \
  modules"

CLEAN="make -C ${kernel_source_dir} \
  M=${dkms_tree}/${PACKAGE_NAME}/${PACKAGE_VERSION}/build \
  clean"
