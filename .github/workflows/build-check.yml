name: build-check

on:
  workflow_dispatch:
  pull_request:
    branches:
      - main
    paths:
      - 'src/**.hip'
      - 'src/**.cpp'
      - 'src/**.h'
      - '**.hip'
      - '**.cpp'
      - '**.h'
      - Makefile

jobs:
  build-check:
    name: build-check for rocm-axiio
    runs-on: ubuntu-latest
    steps:
    - name: Check out code.
      uses: actions/checkout@v5.0.0
    - name: Install rocm
      run: |
        sudo apt-get update
        sudo apt-get install -y \
          curl \
          expect \
          hipcc \
          libcli11-dev \
          libdrm-dev \
          libhsa-runtime-dev \
          libstdc++-14-dev \
          pahole
    - name: List supported GPUs.
      run: |
        make list
      env:
        CLANGXX: /usr/bin/clang++
    - name: Perform clang linting.
      uses: DoozyX/clang-format-lint-action@v0.18.2
      with:
        source: '.'
        exclude: './build ./stebates-* ./stephen-dec5 ./src/include/external'
        extensions: 'cpp,h,hpp,c,cc,hip'
        clangFormatVersion: 18
        style: file
        inplace: false
    - name: Compile code.
      run: |
        mkdir bin
        mkdir lib
        make CXXFLAGS+="--offload-arch=gfx942:xnack+" all
      env:
        HIPCXX: /usr/bin/hipcc
        HIP_INCLUDE_DIR: /usr/include
        LD_LIBRARY_PATH: /opt/rocm/lib:/usr/lib/x86_64-linux-gnu:$LD_LIBRARY_PATH
    - name: Run tester application.
      run: |
        ./bin/axiio-tester -h
      env:
        LD_LIBRARY_PATH: /opt/rocm/lib:/usr/lib/x86_64-linux-gnu:$LD_LIBRARY_PATH
